.sign
    form.signin(name='signin', ng-submit='signin.$valid && signIn()')
        .account-icon: i.md-account-circle
        md-input(
            name='username',
            type='text',
            title='用户名',
            ng-model='username',
            min-length=4,
            max-length=20,
            alphanumeric
            )

            p.input-errmsg(ng-show='signin.username.$error.minLength') 用户名最小长度为4。
            p.input-errmsg(ng-show='signin.username.$error.maxLength') 用户名最长长度为20。
            p.input-errmsg(ng-show='signin.username.$error.alphanumeric') 用户名只允许字母和数字的组合。

        md-input(
            name='password',
            type='password',
            title='密码',
            min-length=6,
            max-length=16,
            ng-model='password',
            )

            p.input-errmsg(ng-show='signin.password.$error.minLength') 密码最小长度为6。
            p.input-errmsg(ng-show='signin.password.$error.maxLength') 密码最长长度为16。
        div
            button.btn.btn-primary.btn-blue.sign-btn(type='submit') 登录
        .sign-helper
            p: a.signup-btn(href='#/signup') 注册
            p: a.forget-btn(href='#/forget-password') 忘记密码

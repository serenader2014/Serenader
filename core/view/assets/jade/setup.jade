.sign
    form.signin(name='setup', ng-submit='setup.$valid && setupBlog()')
        .account-icon: i.md-account-circle
        md-input(
            name='blogname',
            type='text',
            title='博客名称',
            ng-model='blogName'
            )

        md-input(
            name='blogdesc',
            type='text',
            title='博客描述',
            ng-model='blogDesc'
            )
        md-input(
            name='email',
            type='email',
            title='邮箱',
            ng-model='email',
            )

            p.input-errmsg(ng-show='setup.email.$error.email') 请输入有效的邮箱。
        md-input(
            name='username',
            type='text',
            title='用户名',
            ng-model='username',
            min-length=4,
            max-length=20,
            alphanumeric
            )

            p.input-errmsg(ng-show='setup.username.$error.minLength') 用户名最小长度为4。
            p.input-errmsg(ng-show='setup.username.$error.maxLength') 用户名最长长度为20。
            p.input-errmsg(ng-show='setup.username.$error.alphanumeric') 用户名只允许字母和数字的组合。

        md-input(
            name='password',
            type='password',
            title='密码',
            min-length=6,
            max-length=16,
            ng-model='password',
            )

            p.input-errmsg(ng-show='setup.password.$error.minLength') 密码最小长度为6。
            p.input-errmsg(ng-show='setup.password.$error.maxLength') 密码最长长度为16。
        div
            button.btn.btn-primary.btn-blue.sign-btn(type='submit') Set up blog